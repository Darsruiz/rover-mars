import { ConvertDirectionToArrowPipe } from './convert-direction-to-arrow.pipe';

describe('ConvertDirectionToArrowPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDirectionToArrowPipe();
    expect(pipe).toBeTruthy();
  });
});
