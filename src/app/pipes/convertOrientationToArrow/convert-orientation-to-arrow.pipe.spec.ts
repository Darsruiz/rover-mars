import { ConvertOrientationToArrowPipe } from './convert-orientation-to-arrow.pipe';

describe('ConvertOrientationToArrowPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertOrientationToArrowPipe();
    expect(pipe).toBeTruthy();
  });
});
